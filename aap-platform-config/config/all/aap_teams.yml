---
aap_teams_all:
  - name: platform-operators
    organization: Platform
    state: present

  # Team-scoped operator teams are generated by team_onboarding.yml
  # from team_definitions/*.yml files.
  # Admin roles are granted via authenticator maps, not AAP teams.
  # Example:
  # - name: team-alpha-operators
  #   organization: Team-Alpha
  #   state: present
...
