---
aap_organizations_all:
  - name: Platform
    description: "Platform team workspace -- shared EEs, credential types, and admin operations"
    galaxy_credentials:
      - hub-galaxy-rh-certified
      - hub-galaxy-validated
      - hub-galaxy-published
    state: present

  # Team organizations are dynamically generated by the team_onboarding.yml playbook
  # from team_definitions/*.yml files. They are listed here for reference only after onboarding.
  # Example:
  # - name: Team-Alpha
  #   description: "Alpha team automation resources"
  #   state: present
...
