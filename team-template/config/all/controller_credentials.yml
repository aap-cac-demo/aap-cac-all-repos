---
# Team credentials -- define credentials for your automation here.
# For sensitive values, encrypt with ansible-vault and add to config/<env>/secrets.yml.
# The team vault credential (auto-provisioned during onboarding) decrypts them at runtime.
# Update the vault password in AAP UI before first use.
controller_credentials_all: []
  # Example:
  # - name: "app-ssh-key"
  #   organization: "Team-Alpha"
  #   credential_type: "Machine"
  #   description: "SSH key for application servers"
  #   inputs:
  #     username: "deploy"
  #     ssh_key_data: "{{ app_ssh_private_key }}"
  #   state: present
...
